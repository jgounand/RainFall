#include <stdio.h>
#include <stdlib.h>
int p()
{
	char *string;
	void *addr
	fflush(strem);
	gets(str);
	if ((unsigned int)addr & 0xb0000000 == 0xb0000000){
		printf("%p\n",addr);
		exit(1);	
	}
	puts(string);
	strdup(string);
	return ;
}

int main()
{
	return (p());
}
