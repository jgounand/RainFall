#include <stdio.h>
#include <stdlib.h>

void p(char *str)
{
	printf(str);
	return ;
}


void n()
{
	void *addr = 0x8049810;
	char *str;
	fgets(str,0x200,STDIN);
	p(str);
	if (*addr == 0x1025544)
	{	
		system("")
	}
}

int main()
{
	return (n());
}
